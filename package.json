{
  "name": "jquery.tokeninput",
  "version": "2.0.0",
  "scripts": {
    "build": "grunt build",
    "lint": "grunt lint",
    "lint:ci": "grunt jshint:ci",
    "test": "grunt build",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "grunt": "~0.4.2",
    "grunt-cli": "~0.1.13",
    "grunt-contrib-jshint": "~0.6.3",
    "grunt-contrib-nodeunit": "~0.2.0",
    "grunt-contrib-uglify": "~0.3.2"
  }
}
